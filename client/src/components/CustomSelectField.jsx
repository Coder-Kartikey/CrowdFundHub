import { FormControl, InputLabel, Select, MenuItem } from '@mui/material';
import { styled } from '@mui/material/styles';

const CustomSelectField = styled(FormControl)({
  marginBottom: '16px',
  width: '100%',
});

export default CustomSelectField;